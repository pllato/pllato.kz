{
  "//": "Pllato Meet — добавь узел \"meet\" в правила Firebase Realtime Database.",
  "//1": "Firebase Console -> Realtime Database -> Rules. НЕ удаляй свои существующие правила,",
  "//2": "просто добавь блок \"meet\" внутрь \"rules\" рядом с остальными узлами и нажми Publish.",
  "//3": "Гостям аккаунт не нужен — поэтому /meet открыт на чтение/запись (только этот узел).",
  "rules": {
    "meet": {
      ".read": true,
      ".write": true,
      "$room": {
        "presence": {
          "$peer": {
            ".validate": "newData.hasChildren(['name','role'])",
            "name": { ".validate": "newData.isString() && newData.val().length <= 60" }
          }
        },
        "chat":       { "$m": { ".validate": "newData.hasChild('text') && newData.child('text').val().length <= 1000" } },
        "transcript": { "$m": { ".validate": "newData.hasChild('text') && newData.child('text').val().length <= 2000" } },
        "signals":    { "$peer": { "$m": { ".validate": "newData.hasChild('from')" } } }
      }
    }
  }
}
