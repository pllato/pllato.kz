{
  "name": "pllato-hr-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + D1 backend for the Pllato HR assessment panel (Firebase-authenticated team storage of candidate evaluations).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute pllato-hr-d1 --remote --file=schema.sql"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
