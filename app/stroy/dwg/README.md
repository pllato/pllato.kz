# Бесплатный DWG-редактор · прототип 0.1

Отдельное приложение: /app/stroy/dwg/. Вход из меню и стартовой страницы «План-факт».

## Реально реализовано

- Локальное преобразование DWG в DXF через LibreDWG WASM в отдельном Worker.
- Открытие ASCII DXF; просмотр LINE, LWPOLYLINE (включая bulge), CIRCLE, ARC, TEXT, MTEXT, простых вложенных INSERT.
- Слои, панорамирование, зум колесом и двумя пальцами, выбор объекта.
- Перемещение поддерживаемого объекта через DX/DY; правка TEXT, добавление LINE/TEXT, удаление простых объектов.
- Отмена/повтор последних 15 изменений; измерение прямого расстояния с явно выбранными единицами.
- Скачивание отдельного DXF с сохранением неизвестных исходных DXF-записей.

## Ограничения — не скрывать

Запись DWG в готовой сборке **отключена upstream** (configure --disable-write). Кнопки «Сохранить DWG» нет. Результат — DXF, а не DWG с переименованным расширением. DWG-save требует отдельной сборки движка и round-trip проверки в AutoCAD.

Это ограниченный 2D-прототип, не полная CAD-система. Отображается только Model Space. Не отображаются размеры, штриховки, многие 3D/OCS объекты, внешние ссылки, POLYLINE старого типа и прочие неразобранные сущности; список выводится справа. Шрифты системные, MTEXT упрощён, цвета ACI приблизительные. Вставки блоков можно сдвигать целиком, но не удалять или редактировать их содержимое. Геометрия не является основанием для автоматической сметы.

После импорта DWG часть данных может быть потеряна уже на стадии конвертации; сохранение неизвестных DXF-записей не устраняет это ограничение. Оригинал не изменяется. Проверять результат в AutoCAD; не использовать как единственную копию.

Вход до 20 МБ, результат DXF до 64 МБ, отображение до 150 000 обходов объектов, глубина блоков до 12, таймаут чтения 90 секунд. На телефоне рекомендуется отдельный лист до 5 МБ; готовый движок резервирует значительную WASM-память, некоторые телефоны не смогут его запустить.

Файлы не отправляются на сервер. Работа хранится в памяти вкладки; для сохранения скачать DXF. Закрытие вкладки с правками вызывает предупреждение. Существующий gate.js оставлен без изменений.

## Движок и лицензия

@mlightcad/libredwg-web 0.7.14 (GPL-3.0), исходный tag v0.7.14:
1dd682f46339f37b67c5ff1085d10d04a8c16d7e.

Непатченные vendor/libredwg-web.js и .wasm взяты из npm tarball
https://registry.npmjs.org/@mlightcad/libredwg-web/-/libredwg-web-0.7.14.tgz.
Полный соответствующий исходный архив доступен вместе с приложением в
vendor/libredwg-web-source.tar.gz, лицензия — vendor/COPYING.
Upstream: https://github.com/mlightcad/libredwg-web.
Порядок сборки: bindings/javascript/package.json и README внутри архива.

Код этого отдельного редактора (cad.mjs, editor.mjs, worker.mjs, index.html, style.css) предоставляется под GPL-3.0-or-later; без гарантии. Остальные приложения сайта сюда не включены и не перелицензируются.

## Проверка

node --test tests/dwg-core.test.mjs

Браузерный тест: локальный HTTP-сервер + tests/dwg-browser.cjs (Playwright из рабочего окружения). Gate подменяется только для localhost в тестовом браузере; production gate не обходится.
