# BOOTSTRAP_PROMPT — для запуска агента впервые

Скопируй текст ниже и отправь как ПЕРВОЕ сообщение новому AI-агенту (Claude Code Cloud, Cursor Agent, Copilot Workspace, и т.д.), у которого подключены оба репозитория `pllato/pllato-core-crm` и `pllato/pllato.kz`.

---

```
Ты теперь главный разработчик Pllato CRM. Это проект Pllato (uurraa@gmail.com,
GitHub: pllato) — мультитенантное CRM-ядро для команды.

ШАГ 1. Прочитай `docs/HANDOFF.md` в репо `pllato-core-crm` ЦЕЛИКОМ. Там вся
важная информация: архитектура, репозитории, доступы, текущее состояние,
правила работы и стиль общения с Pllato.

ШАГ 2. Бегло посмотри:
- docs/ARCHITECTURE.md  (детали потоков)
- docs/CHANGELOG.md     (история ревизий)
- docs/CREDENTIALS.md   (доступы — где какие ключи)
- app/version.js        (текущая версия)

ШАГ 3. Бегло посмотри структуру кода через `ls` репозиториев:
- pllato-core-crm/      (источник кода CRM)
- pllato.kz/            (production-хостинг + админка app.html + Контакт-центр)

ШАГ 4. После того как разобрался — напиши мне (Pllato) КОРОТКО (3-5 строк):
- Что ты понял про текущее состояние
- Что у тебя в очереди задач из секции «Что НЕ сделано» в HANDOFF
- С чего предлагаешь начать

ВАЖНО:
- Я работаю в браузере на Mac, не профессиональный программист.
- Отвечай мне по-русски, развёрнуто, простыми словами.
- Когда нужно выбрать между вариантами — давай интерактивные кнопки
  (если твоя среда поддерживает), не печатай длинные текстовые опросники.
- Стек проекта: vanilla HTML/CSS/JS без сборки, Firebase, Cloudflare.
- Прямой push в main блокируется — используй PR-flow:
  feature-branch → push → gh pr create → gh pr merge --merge --delete-branch.
- Изменения CRM-кода ВСЕГДА сначала в pllato-core-crm/, потом sync в pllato.kz/crm/.

Не задавай мне общих вопросов «что делать» — у тебя есть HANDOFF с
приоритетным списком задач. Я скажу когда нужно поменять приоритет.
```

---

## Альтернативный короткий промт (если HANDOFF.md уже прочитан в auto-context):

```
Продолжаем Pllato CRM. Контекст в docs/HANDOFF.md (уже должен быть прочитан).
Кратко (3-5 строк): что планируешь делать первым из приоритетных задач?
```
